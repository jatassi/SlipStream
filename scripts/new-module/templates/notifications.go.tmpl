// Code generated by "make new-module". Edit as needed.
package {{.PackageName}}

import "github.com/slipstream/slipstream/internal/module"

var _ module.NotificationEvents = (*Module)(nil)

// {{.Title}} module notification event IDs.
const (
	Event{{.Title}}Added   = "{{.ID}}:added"
	Event{{.Title}}Deleted = "{{.ID}}:deleted"
)

func (m *Module) DeclareEvents() []module.NotificationEvent {
	return []module.NotificationEvent{
		{ID: Event{{.Title}}Added, Label: "{{.Title}} Added", Description: "When a {{.ID}} is added to the library"},
		{ID: Event{{.Title}}Deleted, Label: "{{.Title}} Deleted", Description: "When a {{.ID}} is removed"},
	}
}
