-- Code generated by "make new-module". Edit as needed.
-- name: Get{{.Title}}ByID :one
SELECT * FROM {{.PluralID}} WHERE id = ?;

-- name: List{{.PluralTitle}} :many
SELECT * FROM {{.PluralID}} ORDER BY title;

-- name: Create{{.Title}} :one
INSERT INTO {{.PluralID}} (title, status, monitored, added_at)
VALUES (?, 'missing', ?, datetime('now'))
RETURNING *;

-- name: Update{{.Title}} :exec
UPDATE {{.PluralID}} SET title = ?, monitored = ? WHERE id = ?;

-- name: Delete{{.Title}} :exec
DELETE FROM {{.PluralID}} WHERE id = ?;

-- name: Count{{.Title}}Files :one
SELECT COUNT(*) FROM {{.ID}}_files WHERE {{.ID}}_id = ?;
