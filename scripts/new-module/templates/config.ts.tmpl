// Code generated by "make new-module". Edit as needed.
import { Box } from 'lucide-react'

import type { ModuleConfig } from '../types'

// TODO: implement - replace placeholder query keys with real ones
const {{.ID}}Keys = {
  all: ['{{.PluralID}}'] as const,
  list: () => ['{{.PluralID}}', 'list'] as const,
  detail: (id: number) => ['{{.PluralID}}', id] as const,
}

export const {{.ID}}ModuleConfig: ModuleConfig = {
  id: '{{.ID}}',
  name: '{{.PluralTitle}}',
  singularName: '{{.Title}}',
  pluralName: '{{.PluralTitle}}',
  icon: Box,
  themeColor: 'gray',
  basePath: '/{{.PluralID}}',

  routes: [
    { path: '/', id: '{{.PluralID}}' },
    { path: '/$id', id: '{{.ID}}Detail' },
    { path: '/add', id: 'add{{.Title}}' },
  ],

  queryKeys: {{.ID}}Keys,

  wsInvalidationRules: [
    {
      pattern: '{{.ID}}:(added|updated|deleted)',
      queryKeys: [{{.ID}}Keys.all],
    },
  ],

  filterOptions: [],
  sortOptions: [
    { value: 'title', label: 'Title' },
    { value: 'dateAdded', label: 'Date Added' },
  ],

  tableColumns: { static: [], defaults: [] },

  // TODO: implement - create card and detail components
  cardComponent: () => null,
  detailComponent: () => null,

  api: {
    // TODO: implement - wire up API calls
    list: async () => [],
    get: async () => ({}),
    update: async () => ({}),
    delete: async () => undefined,
    bulkDelete: async () => {},
    bulkUpdate: async () => ({}),
    bulkMonitor: async () => ({}),
    search: async () => undefined,
    refresh: async () => ({}),
    refreshAll: async () => ({}),
  },
}
